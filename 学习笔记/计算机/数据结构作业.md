# 作业题
### 1.1 试分析时间复杂度
sum=1;
for(i=0; sum<n; i++)
	sum+=i;
### 1.2 试给出一个时间复杂度为O(log2 (n))的程序段。
### 2.2.1 指出以下算法中的错误和低效（即费时）之处，并将它改写成一个既正确又高效的算法。（用C++语言）
![[c4f6f93d-35fc-476b-975d-122cd2d2db92.png||300]]
`Status DeleteK(SqList &a, int i, int k) {`
	`//本过程从顺序存储结构的线性表a中删除第i个元素起的k个元素`
	`if(i<1 || k<0 || i+k>a.length)`
		`return INFEASIBLE;`
	`else{`
		`for(count=1; count<k; count++) {`
		`//删除第一个元素`
			`for(j=a.length; j>=i+1; j--)`
				`a.elem[j-1]=a.elem[j];`
			`a.length--;`
		`}`
	`return OK;`
`}//DeleteK`
### 6.1一颗完全二叉树上有1001个结点，其中叶子结点的个数是_______。
A. 250  B. 500  C. 501  D. 505
C
### 6.2在一棵具有n个结点的完全二叉树，树枝结点的最大编号为_______。
$A.\lfloor(n+1)/2\rfloor$  $B.\lfloor(n-1)/2\rfloor$  $C.\lceil n/2\rceil$  $D.\lfloor n/2\rfloor$ 
D

### 6.3一棵度为2的有序树与一棵二叉树有何区别

### 6.4试分别画出具有3个结点的树和3个结点的二叉树的所有不同形态

### 6.5 高度为h的完全二叉树至少有多少个结点?至多有多少个结点?

### 6.6 下述编码哪一组不是前缀码?{00,01,10,11}, {0,1,00,11}, {0,10,110,111}

### 6.7 假设用于通信的电文由字符集{a,b,c,d,e,f,g,h}中的字母构成，这8个字母在电文中出现的概率分别为{0.07, 0.19, 0.02, 0.06, 0.32, 0.03, 0.21, 0.10},为这8个字母设计哈夫曼编码。

![[59f5fed9-2dc3-4391-895a-dfe64e5cc06a.png]]![[9e1e02d0-34d3-4abb-a4e2-85164491b821.png]]
![[cbfb2a8e-00ad-47b3-952b-d3c9973b9330.png]]